{
  "name": "ping-pub-tools",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "jest": {
    "verbose": true
  },
  "devDependencies": {
    "lerna": "^6.4.1",
    "ts-node":"10.9.1",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.4"
    
  },
  "scripts": {
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream --parallel",
    "test": "yarn jest --coverage"
  }
}
